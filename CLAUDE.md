<!-- >>> managed by mswai >>> -->
@AGENTS.md
<!-- <<< managed by mswai <<< -->
프로그램의 구조는 .claude/RhythmGame_Architecture.md 참고

---

# Git 안내 규칙 (commit / push)

**이 프로젝트의 개발자는 git 에 익숙하지 않다.** 명령어를 안다고 가정하지 말고, 매번 짧게
설명을 붙여서 안내한다. "커밋하세요" 라고만 하면 안 된다.

## 1. 언제 안내하는가

아래 시점에 **먼저 제안한다.** 사용자가 요청할 때까지 기다리지 않는다.

- 하나의 작업 단위가 **끝나고 검증까지 통과했을 때** (`## 3. Verify` 완료)
- **`memory.md` 작업 로그에 항목을 추가한 직후** — 기록과 커밋은 한 쌍이다
- 사용자가 "이제 됐다", "잘 된다", "다음 작업하자" 처럼 **한 단계를 마무리하는 신호**를 보냈을 때

반대로 **안내하지 않는 때**:

- 검증 전 / 빌드 에러가 남아 있을 때 → 고장난 상태를 저장하게 된다
- 한 작업 도중 파일 하나 고칠 때마다 → 커밋이 잘게 쪼개져 히스토리가 읽기 어려워진다

## 2. 어떻게 안내하는가

**commit 과 push 는 각각 무엇을 하는지 매번 한 줄로 설명한다.** 둘을 묶어서 "올릴까요?" 라고
뭉뚱그리지 말 것. 사용자가 두 단계의 차이를 아직 모른다는 전제로 쓴다.

- **commit** = 지금까지 바꾼 내용을 **내 컴퓨터에 저장점으로 찍는 것.** 아직 GitHub 에는 안 올라간다.
- **push** = 그 저장점을 **GitHub 에 올려 조원들이 볼 수 있게 하는 것.**

안내 형식:

1. 이번에 무엇을 바꿨는지 **한 줄 요약**
2. 커밋 메시지 **초안을 제시**한다 (사용자가 직접 짜게 하지 않는다)
3. 실행 여부를 묻고, **승인하면 커밋과 푸시를 대신 실행한다**
4. 끝난 뒤 결과를 확인해서 알려준다 (커밋 해시 / `main -> main` 성공 여부)

> 커밋과 푸시는 **사용자 승인 후에 실행**한다. 승인 없이 먼저 저지르지 않는다.

## 3. push 가 거부되면 (가장 흔한 상황)

```
! [rejected]  main -> main (fetch first / non-fast-forward)
```

이 메시지가 나오면 **에러가 아니라 안전장치다.** 다음 순서로 설명한다.

1. **왜 났는지** — "내가 작업하는 동안 **다른 조원이 GitHub 에 먼저 올렸습니다.**
   지금 그대로 올리면 그 사람 작업을 덮어쓰게 되니, git 이 일부러 막은 겁니다."
2. **어떻게 푸는지** — "**`git pull` 로 남의 작업을 먼저 받아온 뒤**, 내 것을 올리면 됩니다."
3. `git pull` → `git push` 를 실행하고 결과를 보고한다.
4. pull 에서 **CONFLICT(충돌)** 가 나면 혼자 처리하지 말고, **어느 파일의 어느 부분이 겹쳤는지
   보여주고** 어느 쪽을 살릴지 사용자에게 물어본다.

> ⛔ **거부됐다고 `--force` / `-f` 를 쓰지 않는다.** 남의 커밋이 사라진다.
> 강제 푸시는 사용자가 명시적으로 요청하고, 그 결과(다른 사람 작업 소실 가능성)를 설명한 뒤에만.

## 4. 커밋 전 항상 확인할 것

- **비밀정보가 섞이지 않았는가** — `.mcp.json` / `.cursor/mcp.json` 에는 API 키가 들어 있다.
  `.gitignore` 로 막혀 있지만, 커밋 전에 스테이징 목록에 없는지 확인한다.
- **`.codeblock` 은 커밋 대상이다.** 자동 생성물이라 무시하고 싶어지지만, `.mlua` 와 짝이 없으면
  스크립트 컴포넌트가 로드 시 조용히 드롭되고 저장 시 영구 소실된다.
- **커밋 메시지는 한국어로, 무엇을 왜 바꿨는지** 적는다. "수정", "업데이트" 같은 건 쓰지 않는다.

## 5. 저장소 정보

- 원격: `https://github.com/greenfrog787-cpu/maple-1-group` (public)
- 브랜치: `main` 하나만 사용
- 이 저장소의 커밋 identity 는 **로컬 설정**으로 `greenfrog787-cpu` 로 고정돼 있다
  (전역 설정은 다른 계정이므로 `git config --global` 을 건드리지 않는다)
- 조원용 사용법은 `README.md` 에 정리돼 있다. 사용자가 git 사용법을 물으면 그쪽을 함께 안내한다.