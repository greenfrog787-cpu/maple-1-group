# 몬스터종류

프로젝트에서 **실제로 쓸 수 있는 몬스터**는 아래 5종임. (`Obstacle`은 몬스터가 아니라 장애물 모델)

## 몬스터 목록

| 모델 ID | 표시 이름 | AI 행동 | 스프라이트 특징 | 모델 위치 |
|---------|-----------|---------|----------------|-----------|
| `staticmonster` | StaticMonster | **고정** (AI 없음) | 별도 스프라이트 (`6c7130f5...`) | 엔진 기본 템플릿 (map01에 배치됨) |
| `movemonster` | MoveMonster | **배회** (`AIWander`) | 배회형 몬스터 (`3e76c89a...`) | `Global/MoveMonster.model` |
| `chasemonster` | ChaseMonster | **추적** (`AIChase`) | 추적형 몬스터 (`6d381bea...`) | `Global/ChaseMonster.model` |
| `mushroom` | mushroom | **추적** (`AIChase`) | ChaseMonster와 동일 외형 | `Global/mushroom.model` |
| `boss` | boss | **배회** (`AIWander`) | MoveMonster와 동일 외형 | `Global/boss.model` |

공통으로 `script.Monster`(HP/사망) + `script.MonsterAttack`(공격) 스크립트가 붙어 있음.

---

## 행동 타입별 분류

**1. 고정형**
- `staticmonster` — 제자리에서 맞기만 하는 타입

**2. 배회형**
- `movemonster` — 좌우로 돌아다님
- `boss` — 이름은 보스지만 AI는 배회형 (`movemonster`와 같은 외형)

**3. 추적형**
- `chasemonster` — 플레이어를 쫓아옴
- `mushroom` — 추적형 커스텀 몬스터 (`chasemonster`와 같은 외형)

---

## 프로젝트에서 쓰이는 곳

**리듬 게임** (`ObstacleManager`) — 화살표 방향별 몬스터 고정 배치:
- ← : `mushroom`
- ↓ : `movemonster`
- ↑ : `chasemonster`
- → : `boss`

**러너 모드** (`AutoRunner`) — 랜덤 스폰:
- `mushroom`, `boss`

**map01** — 템플릿으로 배치된 몬스터:
- StaticMonsterTemplate, MoveMonsterTemplate, ChaseMonsterTemplate, MushroomTemplate

---

## 참고

- `RootDesk/MyDesk/Models/` 아래 **커스텀 몬스터 모델은 아직 없음**. 전부 `Global/` 기본 모델 또는 엔진 템플릿 사용 중.
- `mushroom` / `boss`는 기존 몬스터 외형을 재사용한 **이름만 다른 변형**에 가깝음.
- 리듬 게임에서 스폰할 때는 `ObstacleManager`가 내장 AI(추적/배회)를 꺼서 **순수 판정 타겟**으로 씀.

새 몬스터를 추가하거나, 기존 5종 중 하나를 베이스로 커스텀 모델 만드는 것도 도와줄 수 있음.
